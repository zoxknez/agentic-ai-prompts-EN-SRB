# Gemini CLI — Project Instructions

@AGENTS.md

## Gemini-specific

- Load the matching prompt from `prompts/en/` (or `prompts/sr/`) at the start of each task.
- Use **Compact Mode** (bottom of each prompt file) when token budget is limited.
- Never claim tests passed without running the project's test commands.
- For security audits (02, 05): redact all secrets in output.

Prompt index: see `AGENTS.md`.
